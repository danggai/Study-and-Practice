package com.example.tddexample.util

object CommonFunction {
    fun plus(origin: Int, dest: Int): Int {
        return origin + dest
    }

    fun minus(origin: Int, dest: Int): Int {
        return origin - dest
    }

    fun multiple(origin: Int, dest: Int): Int {
        return origin * dest
    }
}